#include <stdio.h>

/**
* simple test break program
**/
int main()
{
/*	char c;
	int count;

	c = getchar();
	count = 0;
	while (c != EOF)
	{
		putchar(c);
		c = getchar();
		count++;
		printf("%d", count);
		} */
	printf("%s\n", "\0");
}
